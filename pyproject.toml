[project]
name = "futbotmx"
version = "0.1.0"
description = "Computer vision pipeline for robot soccer videos."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
